//write a function call add(a, b)
//write subtract(a, b) that subtracts b from a
//write multiply(a, b)
//write divide(a, b)

//using these functions and only these functions
//write square(number)
//write sumOfSquares(a, b)
    //squares, squares b
    //returns the sum of those numbers

//add funtionality to each of thses functions that checks if both numbers are numeric and outputs
//a string that says "inputs must be numeric" if either of them isn't a number

//add functionality to divide() that checks if the divisor is zero and outputs "divisor must not be zero"




var add = (a + b);

function add (a, b) {
    return a + b
}
function subtract (a, b) {
    return a - b
}
function multiply (a, b) {
    return a * b
}
function divide (a, b) {
    return a / b
}
console.log("the result of adding 1 and 1 is" + add(1, 1));
console.log("2 minus 1 is " + subtract(2,1));
console.log(square(5));
}
function sumOfSquares(a, b) {
    return sum(square(a)), square(b);
)
}
console.log("sum of squares of 2 and 3 are" + sumOfSquares(2, 3));
